package pojos;

public enum RouteType {
    WALKING, CYCLING, DRIVING, TRAIN;
    /**
     * ecah variable is also the variable value as a string
     * each variable is by default static and final
     * the code above compiles into the code below:
     *
     * public static final pojos.RouteType WALKING = new RouteType()
     */
}
